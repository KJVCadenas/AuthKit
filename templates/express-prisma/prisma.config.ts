// Prisma 6 config for datasource connection
import 'dotenv/config';

// Prisma 6 uses prisma/.prismarc or environment variables for configuration
// The schema path and migrations are configured here for reference
// In Prisma 6, most configuration is handled via environment variables
export default {
  schema: 'prisma/schema.prisma',
};
