# Example environment variables for AuthKit Express/Prisma
PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/authkit
JWT_SECRET=REPLACE_WITH_A_RANDOM_32_BYTE_STRING_MINIMUM
JWT_REFRESH_SECRET=REPLACE_WITH_A_RANDOM_32_BYTE_STRING_MINIMUM
# IMPORTANT: Use a cryptographically secure random string (≥32 characters) for each secret.
# Example (Linux/macOS): head -c 32 /dev/urandom | base64
# Example (Windows): powershell -Command "[Convert]::ToBase64String((1..32 | ForEach-Object {Get-Random -Minimum 0 -Maximum 256}))"
CORS_ORIGIN=http://localhost:3000
